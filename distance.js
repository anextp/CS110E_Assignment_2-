const x1=9, y1=2, x2=-3, y2=4;
if(x1>=0 && y1>=0 && x2>=0 && y2>=0)
console.log("The distance is " + (Math.sqrt((x1-x2)**2 + (y1-y2)**2)));
else 
console.log("The coordinates are not in the required quarter");
